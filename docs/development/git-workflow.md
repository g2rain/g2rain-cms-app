# Git 工作流

1. 从目标基线创建小范围分支。
2. 修改前查看工作区，保留其他人的未提交内容。
3. 生成器执行前先提交、暂存或备份，因为它会覆盖已有文件。
4. 提交前检查状态、差异、敏感信息、生成产物和 `git diff --check`。
5. 在 PR 中列出影响领域、运行模式、构建/测试结果、人工验证和已知风险。

建议提交主题保持单一：业务实现、生成结果、架构迁移和文档更新不要混入无关格式化。

